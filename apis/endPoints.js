import {  amazon_URL } from '../config';

export default {
    PATIENT: `${amazon_URL}patient`,
    PROVIDERS: `${amazon_URL}provider`,
    USER: `${amazon_URL}user`,
    SHARE: `${amazon_URL}share`,
    LOG: `${amazon_URL}mobile-logging`,
}
